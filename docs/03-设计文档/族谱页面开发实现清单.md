# 族谱页面开发实现清单

## 📋 开发准备

### ✅ 环境配置检查
- [ ] Node.js 18+ 已安装
- [ ] Vue 3.4+ 项目已创建
- [ ] TypeScript 配置完成
- [ ] Element Plus 2.4+ 已安装
- [ ] G6 4.8+ 图形库已安装
- [ ] Pinia 状态管理已配置
- [ ] Vue Router 路由已配置

### ✅ 依赖包安装
```bash
# 核心依赖
npm install vue@^3.4.0 vue-router@^4.2.0 pinia@^2.1.0
npm install element-plus@^2.4.0 @element-plus/icons-vue@^2.1.0
npm install @antv/g6@^4.8.0

# 工具库
npm install dayjs@^1.11.0 lodash-es@^4.17.0

# 开发依赖
npm install -D typescript@^5.0.0 @vitejs/plugin-vue@^4.5.0
npm install -D sass@^1.69.0 @types/lodash-es@^4.17.0
```

## 🏗️ 第一阶段：基础架构搭建

### 1.1 类型定义 (优先级: 🔴 高)
- [ ] 创建 `src/types/family.ts`
  ```typescript
  export interface FamilyMember {
    id: number
    name: string
    gender: 'male' | 'female'
    birth_date?: string
    death_date?: string
    is_alive: boolean
    generation: number
    avatar?: string
    occupation?: string
    bio?: string
    family_id: number
    father_id?: number
    mother_id?: number
    spouse_ids?: number[]
    children_ids?: number[]
    created_at: string
    updated_at: string
  }

  export interface Family {
    id: number
    name: string
    description?: string
    visibility: 'public' | 'private' | 'family_only'
    creator_id: number
    created_at: string
    updated_at: string
  }
  ```

- [ ] 创建 `src/types/familyTree.ts`
  ```typescript
  export type LayoutType = 'tree-vertical' | 'tree-horizontal' | 'radial' | 'force'

  export interface ViewOptions {
    showPhotos: boolean
    showDates: boolean
    showGeneration: boolean
    showRelationshipLines: boolean
  }

  export interface MemberFilters {
    gender: string[]
    generationRange: [number, number]
    ageFilter: 'all' | 'living' | 'deceased'
    statusFilter: 'all' | 'married' | 'single'
  }

  export interface GraphData {
    nodes: GraphNode[]
    edges: GraphEdge[]
  }

  export interface GraphNode {
    id: string
    data: FamilyMember
    x?: number
    y?: number
  }

  export interface GraphEdge {
    id: string
    source: string
    target: string
    type: 'parent-child' | 'spouse'
  }
  ```

### 1.2 状态管理 (优先级: 🔴 高)
- [ ] 创建 `src/stores/family.ts`
  ```typescript
  import { defineStore } from 'pinia'
  import type { Family, FamilyMember } from '@/types/family'

  export const useFamilyStore = defineStore('family', {
    state: () => ({
      currentFamily: null as Family | null,
      members: [] as FamilyMember[],
      loading: false,
      error: null as string | null
    }),

    getters: {
      getMemberById: (state) => (id: number) => 
        state.members.find(member => member.id === id),
      
      getFilteredMembers: (state) => (filters: MemberFilters) => {
        // 实现筛选逻辑
        return state.members
      },

      getMembersByGeneration: (state) => (generation: number) =>
        state.members.filter(member => member.generation === generation)
    },

    actions: {
      async loadFamily(familyId: string) {
        this.loading = true
        try {
          // API 调用
          this.loading = false
        } catch (error) {
          this.error = error.message
          this.loading = false
        }
      },

      async loadMembers(familyId: string) {
        // 加载成员数据
      },

      async addMember(member: Partial<FamilyMember>) {
        // 添加成员
      },

      async updateMember(member: FamilyMember) {
        // 更新成员
      },

      async deleteMember(memberId: number) {
        // 删除成员
      }
    }
  })
  ```

### 1.3 路由配置 (优先级: 🟡 中)
- [ ] 在 `src/router/index.ts` 中添加族谱页面路由
  ```typescript
  {
    path: '/family/:id/tree',
    name: 'FamilyTree',
    component: () => import('@/pages/family/FamilyTreePage.vue'),
    meta: {
      title: '族谱图',
      requiresAuth: true
    }
  }
  ```

## 🎨 第二阶段：页面布局组件

### 2.1 主页面容器 (优先级: 🔴 高)
- [ ] 创建 `src/pages/family/FamilyTreePage.vue`
  - [ ] 基础布局结构
  - [ ] 响应式数据定义
  - [ ] 生命周期钩子
  - [ ] 事件处理方法
  - [ ] 样式定义

### 2.2 页面头部 (优先级: 🟡 中)
- [ ] 创建 `src/components/family/FamilyTreeHeader.vue`
  - [ ] 返回按钮
  - [ ] 族谱标题显示
  - [ ] 操作按钮组 (分享、设置、邀请)
  - [ ] 响应式样式

### 2.3 功能工具栏 (优先级: 🔴 高)
- [ ] 创建 `src/components/family/FamilyTreeToolbar.vue`
  - [ ] 布局选择器
  - [ ] 世代筛选器
  - [ ] 搜索输入框
  - [ ] 称呼查询按钮
  - [ ] 缩放控制按钮组
  - [ ] 视图选项下拉菜单
  - [ ] 导出功能下拉菜单

### 2.4 侧边栏容器 (优先级: 🟡 中)
- [ ] 创建 `src/components/family/FamilyTreeSidebar.vue`
  - [ ] 折叠/展开功能
  - [ ] 子组件容器布局
  - [ ] 响应式适配

### 2.5 状态栏 (优先级: 🟢 低)
- [ ] 创建 `src/components/family/FamilyTreeStatusBar.vue`
  - [ ] 统计信息显示
  - [ ] 缩放级别显示
  - [ ] 最后更新时间

## 🧩 第三阶段：侧边栏子组件

### 3.1 族谱信息卡片 (优先级: 🟡 中)
- [ ] 创建 `src/components/family/FamilyInfoCard.vue`
  - [ ] 族谱名称和描述
  - [ ] 统计数据展示
  - [ ] 可见性标识

### 3.2 成员筛选器 (优先级: 🟡 中)
- [ ] 创建 `src/components/family/MemberFilters.vue`
  - [ ] 性别筛选标签
  - [ ] 世代范围筛选
  - [ ] 状态筛选 (在世/已故)
  - [ ] 筛选逻辑实现

### 3.3 成员列表 (优先级: 🔴 高)
- [ ] 创建 `src/components/family/MemberList.vue`
  - [ ] 虚拟滚动列表
  - [ ] 成员项组件
  - [ ] 搜索高亮
  - [ ] 选择状态管理
  - [ ] 添加成员按钮

### 3.4 统计信息 (优先级: 🟢 低)
- [ ] 创建 `src/components/family/FamilyStats.vue`
  - [ ] 总成员数
  - [ ] 世代统计
  - [ ] 性别比例
  - [ ] 年龄分布

## 📊 第四阶段：图形展示核心

### 4.1 图形主容器 (优先级: 🔴 高)
- [ ] 创建 `src/components/family/FamilyTreeGraph.vue`
  - [ ] G6 图形实例初始化
  - [ ] 数据转换和处理
  - [ ] 布局算法配置
  - [ ] 事件监听器设置
  - [ ] 缩放和平移控制

### 4.2 节点组件 (优先级: 🔴 高)
- [ ] 创建 `src/components/family/GraphNode.vue`
  - [ ] 节点样式定义
  - [ ] 头像显示逻辑
  - [ ] 信息层次布局
  - [ ] 状态指示器
  - [ ] 交互事件处理
  - [ ] 操作按钮显示

### 4.3 连线组件 (优先级: 🟡 中)
- [ ] 创建 `src/components/family/GraphEdge.vue`
  - [ ] 关系线样式
  - [ ] 不同关系类型区分
  - [ ] 动画效果

### 4.4 图形控制器 (优先级: 🟡 中)
- [ ] 创建 `src/components/family/GraphControls.vue`
  - [ ] 缩放控制
  - [ ] 平移控制
  - [ ] 重置视图
  - [ ] 全屏切换

## 💬 第五阶段：弹窗和对话框

### 5.1 成员详情弹窗 (优先级: 🔴 高)
- [ ] 创建 `src/components/family/MemberDetailDialog.vue`
  - [ ] 基本信息展示
  - [ ] 标签页布局
  - [ ] 家庭关系图
  - [ ] 生平事迹
  - [ ] 照片相册
  - [ ] 操作按钮

### 5.2 添加成员弹窗 (优先级: 🔴 高)
- [ ] 创建 `src/components/family/AddMemberDialog.vue`
  - [ ] 表单字段定义
  - [ ] 表单验证规则
  - [ ] 头像上传功能
  - [ ] 关系选择器
  - [ ] 提交处理逻辑

### 5.3 编辑成员弹窗 (优先级: 🔴 高)
- [ ] 创建 `src/components/family/EditMemberDialog.vue`
  - [ ] 预填充表单数据
  - [ ] 字段修改权限控制
  - [ ] 更新提交逻辑

### 5.4 关系查询抽屉 (优先级: 🟡 中)
- [ ] 创建 `src/components/family/RelationshipDrawer.vue`
  - [ ] 成员选择器
  - [ ] 关系计算显示
  - [ ] 关系路径可视化
  - [ ] 称呼结果展示

### 5.5 右键菜单 (优先级: 🟢 低)
- [ ] 创建 `src/components/family/ContextMenu.vue`
  - [ ] 菜单项定义
  - [ ] 位置计算
  - [ ] 权限控制

## 🔧 第六阶段：工具函数和服务

### 6.1 图形工具 (优先级: 🔴 高)
- [ ] 创建 `src/utils/graph.ts`
  ```typescript
  export function convertMembersToGraphData(members: FamilyMember[]): GraphData
  export function calculateNodePositions(nodes: GraphNode[], layout: LayoutType): GraphNode[]
  export function findShortestPath(from: string, to: string, edges: GraphEdge[]): string[]
  export function calculateRelationship(path: string[], members: FamilyMember[]): string
  ```

### 6.2 日期工具 (优先级: 🟡 中)
- [ ] 创建 `src/utils/date.ts`
  ```typescript
  export function formatDate(date: string, format: string): string
  export function calculateAge(birthDate: string, deathDate?: string): number
  export function getChineseEra(date: string): string
  ```

### 6.3 导出工具 (优先级: 🟡 中)
- [ ] 创建 `src/utils/export.ts`
  ```typescript
  export function exportToPNG(canvas: HTMLCanvasElement): void
  export function exportToPDF(data: GraphData): void
  export function exportToExcel(members: FamilyMember[]): void
  ```

### 6.4 API 服务 (优先级: 🔴 高)
- [ ] 创建 `src/api/family.ts`
  ```typescript
  export const familyApi = {
    getFamily: (id: string) => Promise<Family>
    getMembers: (familyId: string) => Promise<FamilyMember[]>
    addMember: (member: Partial<FamilyMember>) => Promise<FamilyMember>
    updateMember: (member: FamilyMember) => Promise<FamilyMember>
    deleteMember: (id: number) => Promise<void>
    calculateRelationship: (fromId: number, toId: number) => Promise<string>
  }
  ```

## 🎯 第七阶段：交互功能实现

### 7.1 基础交互 (优先级: 🔴 高)
- [ ] 节点点击选择
- [ ] 节点双击查看详情
- [ ] 节点拖拽移动
- [ ] 画布缩放和平移
- [ ] 键盘快捷键支持

### 7.2 高级交互 (优先级: 🟡 中)
- [ ] 多选功能 (Ctrl+点击)
- [ ] 框选功能 (拖拽选择)
- [ ] 右键菜单
- [ ] 关系线创建
- [ ] 搜索高亮

### 7.3 触摸交互 (优先级: 🟡 中)
- [ ] 移动端手势支持
- [ ] 双指缩放
- [ ] 长按菜单
- [ ] 滑动导航

## 📱 第八阶段：响应式适配

### 8.1 桌面端优化 (优先级: 🟡 中)
- [ ] 大屏幕布局优化
- [ ] 鼠标交互优化
- [ ] 键盘导航支持

### 8.2 平板端适配 (优先级: 🟡 中)
- [ ] 中等屏幕布局调整
- [ ] 触摸交互优化
- [ ] 侧边栏自适应

### 8.3 移动端适配 (优先级: 🔴 高)
- [ ] 小屏幕布局重构
- [ ] 抽屉式侧边栏
- [ ] 工具栏简化
- [ ] 节点大小调整
- [ ] 手势操作支持

## ♿ 第九阶段：无障碍支持

### 9.1 键盘导航 (优先级: 🟡 中)
- [ ] Tab 键遍历
- [ ] 方向键导航
- [ ] Enter/Space 激活
- [ ] Esc 取消操作

### 9.2 屏幕阅读器 (优先级: 🟡 中)
- [ ] ARIA 标签添加
- [ ] 语义化 HTML
- [ ] 焦点管理
- [ ] 状态通知

### 9.3 视觉辅助 (优先级: 🟢 低)
- [ ] 高对比度模式
- [ ] 字体大小调节
- [ ] 色盲友好配色

## 🚀 第十阶段：性能优化

### 10.1 渲染优化 (优先级: 🟡 中)
- [ ] 虚拟滚动实现
- [ ] 图形懒加载
- [ ] 节点复用机制
- [ ] 层次细节渲染

### 10.2 内存优化 (优先级: 🟡 中)
- [ ] 事件监听器清理
- [ ] 图片资源管理
- [ ] 数据缓存策略
- [ ] 组件销毁处理

### 10.3 网络优化 (优先级: 🟢 低)
- [ ] 数据分页加载
- [ ] 图片压缩和懒加载
- [ ] API 请求缓存
- [ ] 离线数据支持

## 🧪 第十一阶段：测试和调试

### 11.1 单元测试 (优先级: 🟡 中)
- [ ] 组件测试用例
- [ ] 工具函数测试
- [ ] 状态管理测试
- [ ] API 服务测试

### 11.2 集成测试 (优先级: 🟡 中)
- [ ] 页面交互测试
- [ ] 数据流测试
- [ ] 路由导航测试

### 11.3 E2E 测试 (优先级: 🟢 低)
- [ ] 用户流程测试
- [ ] 跨浏览器测试
- [ ] 性能测试
- [ ] 无障碍测试

## 📝 开发注意事项

### ⚠️ 重要提醒
1. **数据安全**: 确保敏感信息不在前端暴露
2. **性能考虑**: 大型族谱数据的渲染优化
3. **兼容性**: 主流浏览器兼容性测试
4. **用户体验**: 加载状态和错误处理
5. **代码质量**: TypeScript 类型安全和 ESLint 规范

### 🔍 调试技巧
1. **Vue DevTools**: 组件状态和事件调试
2. **G6 调试**: 图形渲染和布局调试
3. **网络面板**: API 请求和响应监控
4. **性能面板**: 渲染性能分析

### 📚 参考资源
- [Vue 3 官方文档](https://vuejs.org/)
- [Element Plus 组件库](https://element-plus.org/)
- [G6 图可视化](https://g6.antv.vision/)
- [Pinia 状态管理](https://pinia.vuejs.org/)

## ✅ 完成检查清单

### 功能完整性
- [ ] 所有核心功能已实现
- [ ] 所有交互功能正常
- [ ] 所有弹窗和对话框可用
- [ ] 响应式布局适配完成

### 代码质量
- [ ] TypeScript 类型检查通过
- [ ] ESLint 代码规范检查通过
- [ ] 单元测试覆盖率达标
- [ ] 性能指标满足要求

### 用户体验
- [ ] 加载状态友好
- [ ] 错误处理完善
- [ ] 操作反馈及时
- [ ] 无障碍支持到位

---

**开发建议**: 按照优先级顺序进行开发，先完成核心功能，再逐步添加高级特性。每个阶段完成后进行测试，确保功能稳定后再进入下一阶段。